package templates

import "net/http"

templ Hello(r *http.Request) {
    <li>Name: { r.PostFormValue("service-name") } </li>
    <li>Host: { r.PostFormValue("service-host") }:{ r.PostFormValue("service-port") } </li>
}